add_library(StandardComponents
    MeshComponent
    TransformComponent
    PhysicsComponent
)
target_include_directories(StandardComponents PUBLIC include .)
target_link_libraries(StandardComponents PUBLIC rte)